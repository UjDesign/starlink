{
  "name": "starlink-mvp",
  "version": "1.0.0",
  "description": "Web3短视频平台星链MVP",
  "scripts": {
    "postinstall": "cd contracts && npm install && cd ../backend && npm install && cd ../frontend && npm install",
    "deploy": "./deploy.sh",
    "test": "cd tests && npm test"
  },
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  },
  "devDependencies": {
    "npm-check-updates": "^16.14.20"
  }
}
    